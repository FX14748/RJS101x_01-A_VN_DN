/*import * as ActionTypes from './ActionTypes';


export const SearchedStaff = (state = '', action) => {
    switch (action.type) {
        case ActionTypes.SEARCH_STAFF:
            var searchedStaff = action.payload;
            console.log("SearchedStaff: ", searchedStaff);
            return searchedStaff;
        default:
          return state;
      }
};
*/